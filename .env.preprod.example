# ==========================================
# PREPROD ENVIRONMENT CONFIGURATION (.env)
# ==========================================


# --- SYSTEM ---
ENV=production
# Le port interne sur lequel l'API Go tourne dans le conteneur Docker
PORT=8080

# --- URLS PUBLIQUES ---
# Ce sont les URL que Cloudflare expose. 
# Le backend a besoin de FRONTEND_URL pour les emails (CORS, liens de validation)
FRONTEND_URL=https://preprod.framerate.alexandre-brozzu.fr
# Le frontend (quand il build) a besoin de VITE_API_URL pour savoir où taper :
VITE_API_URL=https://api-preprod.framerate.alexandre-brozzu.fr/api

# --- DATABASE (POSTGRESQL) ---
# Dans le docker-compose, le host s'appelle "postgres"
POSTGRES_USER=framerate_db_user
POSTGRES_PASSWORD=replace_with_a_strong_password
POSTGRES_DB=framerate_preprod
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# --- REDIS CACHE ---
REDIS_URL=redis:6379
REDIS_PASSWORD=

# --- SECRETS & AUTHENTICATION ---
# Utilisez 'openssl rand -base64 32' pour en générer une vraie
JWT_SECRET=replace_this_with_a_very_long_random_string_for_preprod

# --- EXTERNAL APIS ---
# Mailer (Resend)
RESEND_API_KEY=votre_clef_api_resend_ici

# TMDB API
TMDB_API_KEY=votre_clef_api_tmdb_ici
TMDB_BASE_URL=https://api.themoviedb.org/3
TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p
